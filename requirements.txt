# FraudShield — pinned with compatible-range constraints.
# Install inside WSL2 with Python 3.11:
#   python3.11 -m venv .venv && source .venv/bin/activate
#   pip install -r requirements.txt
# For CUDA builds of torch, install torch FIRST from the official index:
#   pip install "torch>=2.2,<3" --index-url https://download.pytorch.org/whl/cu121

# --- Core ML / Training ---
torch>=2.2,<3
opacus>=1.4,<2
scikit-learn>=1.4,<2
imbalanced-learn>=0.12,<1     # non-DP baseline ONLY — never on DP clients (breaks DP guarantee)
numpy>=1.26,<3
pandas>=2.2,<3

# --- Federated Learning ---
safetensors>=0.4,<1

# --- Backend / Server ---
fastapi>=0.110,<1
uvicorn[standard]>=0.29,<1
pydantic>=2.6,<3
pydantic-settings>=2.2,<3
httpx>=0.27,<1
redis>=5,<6
mlflow>=2.11,<4

# --- Data acquisition ---
kagglehub>=0.2,<1

# --- Dev / Test ---
pytest>=8,<9
pytest-asyncio>=0.23,<1
ruff>=0.4
mypy>=1.9
